- route:
    id: demo
    from:
      uri: timer:tick
      parameters:
        period: 10s
    steps:
      - setBody:
          simple: "Hello from Karavan ${date:now:yyyy-MM-dd HH:mm:ss}"
      - log:
          message: "${body}"
