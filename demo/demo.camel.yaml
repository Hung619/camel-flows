- route:
    id: route-186d
    from:
      id: from-4c5c
      uri: timer
      parameters:
        period: 10000
        timerName: tick
    steps:
      - undefined:
          log:
            message: ✅ ay oyy ${date:now:yyyy-MM-dd HH:mm:ss}
