- route:
    id: route-186d
    from:
      id: from-3c94
      uri: timer
      parameters:
        period: 10000
        timerName: tick
      steps:
        - log:
            id: log-0a7f
            message: đã reload  ${date:now:yyyy-MM-dd HH:mm:ss}
